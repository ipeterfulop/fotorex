<a href="{{ $slide->url }}" target="{{ $slide->open_in }}" class="flex items-start justify-start w-full h-full" data-s="@json($slide)">
    <img src="{{ $slide->image_url }}" class="w-full h-full">
</a>
